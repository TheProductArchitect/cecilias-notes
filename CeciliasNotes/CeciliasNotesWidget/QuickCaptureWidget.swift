//
//  QuickCaptureWidget.swift
//  CeciliasNotesWidget
//
//  Retired during the design-language refresh. The lock-screen
//  quick-capture surface is now `LockCircularNewNoteWidget` (and its
//  rectangular / inline companions) in `NewNoteWidget.swift`.
//
//  Kept as an empty file so the Xcode target membership doesn't need
//  to change — the file is still part of the CeciliasNotesWidget target's
//  Compile Sources, it just contributes no symbols now.
//

import WidgetKit
import SwiftUI
